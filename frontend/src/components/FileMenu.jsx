import { Menu } from "@mui/material";
import React from "react";

const FileMenu = ({ anchorEl, open }) => {
  return (
    <Menu anchorEl={anchorEl} open={open}>
      FileMenu
    </Menu>
  );
};

export default FileMenu;
